Installation
============

You can install pynlcd using pip:

.. code-block:: bash

   pip install pynlcd

Requirements
------------

pynlcd requires the following libraries:

- GDAL
- NumPy

Make sure you have GDAL installed on your system before installing pynlcd.